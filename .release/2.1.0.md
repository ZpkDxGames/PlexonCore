# PlexonCore 2.1.0

PlexonCore 2.1.0 hardens the shared runtime foundation used by the Plexon plugin family while preserving compatibility with the established API 2.0 surface and API 1.0 bridge.

## Highlights

- Owner-scoped lifecycle cleanup for event subscriptions, player watches, GUI sessions, and scheduled work.
- Contributor-based Core health aggregation with clearer degraded/failed state reporting.
- Hardened scheduler observability, cancellation, repeating-task ownership, shutdown behavior, and failure recovery.
- Block-origin persistence with coalesced last-write-wins buffering, pressure flushes, retry visibility, and bounded shutdown.
- Lifecycle-owned SQLite writers with transaction rollback/recovery and future-schema refusal.
- Capability-aware integration discovery and lifecycle refresh.
- GUI generation/session validation with stale-session rejection and permission-safe deferred actions.
- Cached PlaceholderAPI adapter lifecycle to remove reflective discovery from rendering hot paths.
- Deterministic item identity/fingerprint behavior.
- Bounded event dedupe without whole-map hot-path scans.
- Validated immutable configuration generations with explicit restart-required settings.
- Additive Core API 2.1 contract.

## Compatibility

PlexonCore 2.1.0 retains the existing API 2.0 surface and the API 1.0 compatibility bridge.

The release candidate was compiled/tested against the production plugin set:

- PlexonUtility
- PlexonSpawners
- PlexonKeys
- PlexonChats
- PlexonQuests
- PlexonRanks
- PlexonShops
- PlexonSkills
- PlexonTools
- PlexonHomes
- PlexonTravel
- PlexonJobs
- PlexonBackpacks
- PlexonGPFlags

The compatibility matrix completed successfully across all 14 repositories without committing downstream source changes.

## Runtime target

- Paper: 26.2
- Java: 25
- Java class major: 69

## Migration

PlexonCore 2.1.0 is intended as an in-place upgrade from the 2.0.5 stable line. Existing persisted data is retained and migrated through the supported schema path. Future/unsupported schema versions fail closed rather than being silently rewritten.

Back up the server before upgrading. Do not perform plugin hot-swaps; stop the server, replace the Core JAR, then start normally.

## Rollback

The known-good rollback baseline is PlexonCore 2.0.5.

If rollback is required after deployment, stop the server before replacing the JAR. Preserve a backup of the PlexonCore data directory taken before the 2.1.0 upgrade.

## Verification

The stable release is published only after:

- complete Core CI passes;
- downstream compatibility passes;
- the exact release-candidate binary passes live PlexonCraft runtime certification;
- the required runtime soak shows no unresolved HIGH/CRITICAL defect;
- the stable-version commit passes the complete CI and compatibility gates again.

The published GitHub Release includes the final JAR and SHA-256 checksum file.
